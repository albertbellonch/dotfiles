## Setting up

* Ensure Ruby and brew are installed
* Run `brew install cowsay fortune wget zsh-autosuggestions`
* Run `gem install lolcat`
* Set the `SEQURA_MACHINE` environment variable to `true` if you are on a Sequra machine